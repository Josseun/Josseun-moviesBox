// import { Outlet } from "react-router";
import Navbar from "../compoents/Navbar";

const MainLayout = () => {
  return (
    <div>
      <nav>
        <Navbar />
      </nav>

      {/* <Outlet /> */}
    </div>
  );
};

export default MainLayout;
